export interface WeakNetConf {
    version: string
    cdn_url: string
    root_url: string
    config_url: string
    is_local_game: boolean;
    is_normal_login: boolean;
    openId: string;
    ip_api: string;
}